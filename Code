//For ultrasonic sensor connection

int trig1 = 2;
int echo1 = 3;
int trig2 = 4;
int echo2 = 5;
int trig3 = 6;
int echo3 = 7;

//For motor

int motorpin1 = 8;
int motorpin2 = 9;
int motorpin3 = 10;
int motorpin4 = 11;

//For ultrasonic sensor

int duration1 = 0;
int duration2 = 0;
int duration3 = 0;

int distance1 = 0;//left sensor
int distance2 = 0;//middle sensor
int distance3 = 0;//right sensor

void setup()
{
  pinMode(trig1 , OUTPUT);
  pinMode(echo1 , INPUT);
  pinMode(trig2 , OUTPUT);
  pinMode(echo2 , INPUT);
  pinMode(trig3 , OUTPUT);
  pinMode(echo3 , INPUT);

  pinMode(motorPin1 , OUTPUT);
  pinMode(motorPin2 , OUTPUT);
  pinMode(motorPin3 , OUTPUT);
  pinMode(motorPin4 , OUTPUT);

  Serial.begin(9600);

}

void loop() {

  digitalWrite(trig1 , HIGH);
  delayMicroseconds(1000);
  digitalWrite(trig1 , LOW);

  duration1 = pulseIn(echo1 , HIGH);
  distance1 = (duration1 / 2) / 28.5 ;// calculate distance in CM
  //Serial.println(distance1);


  digitalWrite(trig2 , HIGH);
  delayMicroseconds(1000);
  digitalWrite(trig2 , LOW);

  duration2 = pulseIn(echo2 , HIGH);
  distance2 = (duration2 / 2) / 28.5 ;// calculate distance in CM
  //Serial.println(distance2);


  digitalWrite(trig3 , HIGH);
  delayMicroseconds(1000);
  digitalWrite(trig3 , LOW);

  duration3 = pulseIn(echo3 , HIGH);
  distance3 = (duration3 / 2) / 28.5 ;// calculate distance in CM
  //Serial.println(distance3);

  if ( distance1==0 && distance3==0) {

    digitalWrite(motorPin1, HIGH);
    digitalWrite(motorPin2 , LOW);
    delay(1000);
  }
  else {
    Serial.print(distance);
    digitalWrite(motorPin1, HIGH);
    digitalWrite(motorPin2 , LOW);
    digitalWrite(led1 , LOW);
    digitalWrite(led2 , HIGH);
    Serial.println("cm   MOTOR START");
    delay(1000);
  }
}
}
